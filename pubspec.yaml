name: flutter_tiny_wavpack_decoder
description: "Decode WavPack (.wv) audio to PCM .wav on-device via the tiny dependency-free WavPack decoder over Dart FFI. Android, iOS, macOS, Linux, Windows."
version: 1.0.0
repository: https://github.com/JairajJangle/flutter_tiny_wavpack_decoder
issue_tracker: https://github.com/JairajJangle/flutter_tiny_wavpack_decoder/issues
topics:
  - audio
  - wavpack
  - codec
  - decoder
  - ffi

environment:
  sdk: ^3.9.0
  flutter: '>=3.27.0'

dependencies:
  ffi: ^2.1.3
  flutter:
    sdk: flutter
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  # This section identifies this Flutter project as an FFI plugin: the
  # native decoder in src/ is built and bundled by each platform's tooling
  # (CMake on Android/Linux/Windows, CocoaPods on iOS/macOS). There is no
  # method-channel platform code.
  plugin:
    platforms:
      android:
        ffiPlugin: true
      ios:
        ffiPlugin: true
      linux:
        ffiPlugin: true
      macos:
        ffiPlugin: true
      windows:
        ffiPlugin: true
