// Umbrella header required by SwiftPM for C targets. This is an FFI plugin:
// Dart binds the exported C symbols (see src/ftwd_shim.h) at runtime, so no
// declarations need to be visible to Swift or Objective-C.
#ifndef FLUTTER_TINY_WAVPACK_DECODER_H
#define FLUTTER_TINY_WAVPACK_DECODER_H
#endif
